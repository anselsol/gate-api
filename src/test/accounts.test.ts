/* eslint-disable @typescript-eslint/no-non-null-assertion, no-console */
import { expect } from 'chai';

describe('Wallet connect', async () => {
  before(async () => {
  });

  describe('Sign in', async () => {
    it('loads the correct wallet and returns an auth token', async () => {
      expect(1).to.equal(1)
    });
  });
});